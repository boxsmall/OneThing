# 「一件」APP

## 前端调整与交互实现清单

> V1.0 · Android 客户端 / 前端联调用

> 开发决策修订：2026-09-02 · 以《一件_APP_V1_开发决策基线.md》为准

> 产品原则：一次只坚持一件事。一天只记录一次。每天操作尽量控制在 3 秒内。

| 项目 | 内容 |
| --- | --- |
| 产品工作名 | 一件 |
| applicationId | `com.boxsmall.onething` |
| 设计方向 | A：温暖极简 |
| 基准画板 | 360 × 800（Android 主流机型适配基准） |
| Figma 文件 | https://www.figma.com/design/XEHhVWEe14BACPoGeKYD76 |
| 文档用途 | 后续由安卓前端/客户端开发按本文对现有原型进行精修、联调与验收 |

核心目标不是增加功能，而是把现有 11 个状态做完整、做统一、做顺畅。

## 1. 本轮调整范围与优先级

> 本轮不新增产品功能。所有开发资源优先用于：视觉统一、交互闭环、状态正确、Android 适配与体验细节。

| 优先级 | 定义 | 本轮要求 |
| --- | --- | --- |
| P0 | 必须完成 | 影响核心闭环、点击可用性、状态展示、数据正确性、Android 适配的问题。上线前必须全部通过。 |
| P1 | 建议完成 | 提升完成感、视觉一致性和产品质感的问题。建议在首版一并完成。 |
| P2 | 后续优化 | 非核心动画、进一步组件化、体验微调。不能阻塞 V1 上线。 |

### 1.1 P0 必改项总览

- 11 个页面/状态均统一为同一套 Android 视觉规范，不允许出现不同字号体系、不同圆角体系或不同主色。

- 所有可点击入口必须有明确点击热区，最小 48dp × 48dp；不能只让文字或细小图标本体可点。

- 首次进入 → 创建目标 → 首页未完成 → 完成 → 已完成 → 记录，主链路必须全程可走通。

- 设置 → 结束这件事 → 结果页 → 历史 → 开始下一件事，支链路必须全程可走通。

- 中断后连续天数重新计算，但累计完成/最长连续不能被清空。

- 当天只能完成一次；完成后不能再次重复计数。

- 所有页面适配状态栏、导航栏/手势区、异形屏安全区，不允许按钮贴底或被系统区域遮挡。

### 1.2 本轮明确不做

- 多目标并行、任务列表、目标分类。

- 社区、好友监督、排行榜。

- 积分、金币、商城、复杂徽章体系。

- AI 对话、AI 建议。

- 复杂数据图表、周报/月报。

- 强制登录、手机号注册。

- 任何文字记录、备注、日记、照片、附件或补签体系。

## 2. 全局 UI 规范（开发统一执行）

| 项目 | 规范 | 开发验收 |
| --- | --- | --- |
| 设计基准 | 360 × 800 | 以 dp 体系实现，不能写死为仅适配单一设备分辨率。 |
| 页面背景 | #FDFBF7 | 所有主页面统一暖白底，不使用纯白大底替代。 |
| 主文字 | #141414 | 标题、主数据、核心操作使用。 |
| 次级文字 | #6B6B6B | 说明、标签、辅助信息使用。 |
| 未完成主色 | #FABF45 | 仅用于未完成态、主按钮、进度点等重要元素。 |
| 完成/成功色 | #59AD5E | 仅用于完成态、成功反馈、已完成日期。 |
| 分割/描边 | #E6E0D6 | 输入框、卡片、弱分割使用。 |
| 主按钮圆角 | 18dp | 全局一致；高度建议 52dp。 |
| 卡片/输入框圆角 | 16dp | 所有卡片与输入框统一。 |
| 页面左右边距 | 24dp | 核心内容保持同一纵向基线。 |
| 字体 | Noto Sans SC / 系统中文无衬线 | Android 实现优先系统字体或项目内统一字体，禁止页面间混用。 |

### 2.1 推荐字号层级

| 层级 | 推荐字号 | 用途 |
| --- | --- | --- |
| 大任务标题 | 28sp / Bold | 如“走路 20 分钟” |
| 关键数据 | 48–52sp / Bold | 连续天数 |
| 页面标题 | 20–24sp / Bold | 创建目标、记录、设置等 |
| 主要按钮 | 16–17sp / Bold | 我完成了 / 开始坚持 |
| 正文 | 14–16sp | 列表内容、结果说明 |
| 辅助文字 | 12–14sp | 日期、说明、标签 |

### 2.2 Android 适配要求（P0）

- 内容区域必须避开状态栏和底部手势区；使用 WindowInsets / Safe Area 思路处理。

- 360dp 宽度为基准，左右边距 24dp；更宽屏幕保持内容居中，不无限拉伸核心按钮。

- 小屏设备优先保证主按钮、任务标题、进度条可见；次要说明允许收缩间距，但不能裁切。

- 所有文本需支持系统字体缩放下的基本可读性，避免固定高度导致中文文字被裁切。

- 点击热区与视觉元素分离：齿轮、返回箭头、列表箭头等视觉尺寸可小，但可点击区域必须扩大。

## 3. 页面清单与逐页调整要求

| 页面/状态 | 优先级 | 出现条件 | UI 调整重点 | 交互要求 |
| --- | --- | --- | --- | --- |
| 01 欢迎页 | P0 | 首次打开时展示。 | Logo/“1.”作为视觉中心；文案仅保留“一件”“一次，只坚持一件事。”；底部单一按钮“开始使用”。 | 点击“开始使用” → 02 创建目标。 |
| 02 创建目标 | P0 | 首次创建或结束上一目标后重新创建。 | 标题“你想坚持什么？”；目标输入框；“提醒我”开关默认开启；默认时间 20:00；底部“开始坚持”；示例标签只做辅助；存在历史时显示弱入口“查看过去的坚持”。 | 未输入目标时按钮不可用；关闭提醒后隐藏/禁用时间选择且不申请权限；点击开始坚持 → 03 首页未完成。 |
| 03 首页·未完成 | P0 | 当天尚未完成。 | 顶部日期 + 设置入口；“今天”；任务标题；任务图标；连续天数；7 日进度；底部“我完成了”；“查看记录”。 | 点击“我完成了”只触发一次；进入 04 完成反馈。设置 → 08；查看记录 → 06。 |
| 04 完成反馈 | P0（状态与 Overlay）；P1（动画与触觉） | Completion 成功落库后的即时反馈状态。 | 首页上方遮罩 + 中央成功卡片；绿色对勾；“今天完成”“连续第 N 天”；文案克制。Figma 可用独立画板表达，但客户端实现为首页 Overlay。 | 不进入独立返回栈；约 1.2 秒自动关闭并呈现 05；允许点击卡片/遮罩立即关闭。 |
| 05 首页·已完成 | P0 | 当天已完成。 | 任务内容不变；主视觉由黄色切换为绿色；显示“今天已完成”“连续第 N 天”；不再出现可重复完成按钮。 | 当天再次进入始终保持此状态；查看记录 → 06；设置 → 08。 |
| 06 坚持记录 | P0 | 查看当前目标历史。 | 周一起始的 7 列自适应月历 + 当前连续 / 累计完成 / 最长连续；底部展示开始日期与当前任务；月历和统计卡片不得重叠。 | 已完成=绿色实心点；未完成=空心；今天=绿色描边高亮；开始日前和未来日期为中性空状态。返回 → 对应首页状态。 |
| 07 中断后首页 | P0 | 目标非今天新建、昨天未完成且今天尚未完成；连续漏掉多天仍使用本状态。 | gapDays=1 显示“昨天没有完成”“今天继续”；gapDays>1 显示“有几天没有继续了”“今天回来就好”；不使用红色失败提示，不突出“连续 0 天”。 | 今天完成后：连续天数从 1 重新计数；累计完成 +1；最长连续仅在超过历史值时更新。 |
| 08 设置 | P0 | 当前目标的轻量设置。 | 当前坚持、提醒时间、提醒开关、我的历史、结束这件事、关于/隐私。 | 当前坚持与提醒可修改；历史 → 11；结束这件事 → 09。返回 → 首页。 |
| 09 结束这件事确认 | P0 | 防止误结束。 | 展示当前坚持天数、累计完成、最长连续；“继续坚持”使用明确可点击的安全样式，危险操作“结束这件事”使用红色。 | 本页已是确认步骤，不再增加第三层弹窗；返回和继续坚持回设置，结束 → 10。 |
| 10 结束结果页 | P0 | 一段坚持正式结束。 | 展示“持续 N 天”和“完成 N 天”；累计完成为 0 时使用“这段记录已保存”，否则使用“你完成了一段坚持”；主按钮“开始下一件事”。 | 开始下一件事 → 02，并清除结束流程返回栈；查看历史 → 11。 |
| 11 我的历史 | P0（列表、数据与返回）；P1（空状态与视觉） | 保存已结束目标。 | 按结束日期倒序展示目标名、起止日期、持续天数、完成天数；卡片高度自适应，不裁切第三行。 | 点击历史条目 V1 可仅查看/无详情；从 08 进入则返回 08，从 10 进入则返回 10。 |

## 4. 首页重点精修规范

> 首页是产品 80% 的体验。后续如果开发时间有限，优先把首页未完成态与已完成态做到最好。

### 4.1 首页未完成态

1. 顶部区域：状态栏由系统管理；内容首行展示日期，设置使用齿轮图标，不再使用“设置 ›”文字作为最终视觉。

2. 任务区域：“今天”弱于任务标题；任务标题使用 28sp Bold，保持单一主焦点。

3. 任务图标：使用固定品牌 `1.` 图形并放在浅暖黄色圆角底上；不提供用户选择图标，不使用 Emoji；设计图中的空白方块和竖线不是正式资源。

4. 连续天数：数字为视觉第二层级。今天未完成但昨天已完成时使用“已经连续完成 N 天”；今天完成后使用“连续第 N 天”；昨天也未完成时不突出“连续 0 天”。

5. 7 日进度：固定显示过去 6 天 + 今天，从旧到新；每一天同时有状态点 + 星期标签；未完成今天使用空心黄色描边；目标开始日前使用中性空状态。

6. 主按钮：“我完成了”高度 52dp，左右 24dp，黄色背景，深色/黑色按钮文字可优先于白字以保证视觉稳定。

7. 按钮底部说明“今天只做好这一件事”弱化显示；“查看记录”独立可点击。

### 4.2 首页已完成态

1. 任务标题、日期布局与未完成态保持完全一致，避免完成后页面结构突然跳动。

2. 任务图标保持原位；在中部使用绿色对勾作为完成反馈。

3. 主文案切换为“今天已完成”，成功色 #59AD5E。

4. 7 日进度全部按真实数据绘制；今天使用绿色圆环/双层点高亮。

5. 不再提供“我完成了”按钮，防止重复提交。

6. 可显示一张极轻的鼓励卡片，例如“太棒了！明天继续。”，但不能盖过核心任务信息。

## 5. 核心交互与状态逻辑

### 5.1 主流程

| 阶段 | 页面状态 | 下一步 |
| --- | --- | --- |
| 首次安装 | 欢迎页 | 点击开始使用 |
| 创建目标 | 输入目标 + 默认开启的提醒，可选择关闭 | 点击开始坚持 |
| 当天未完成 | 首页未完成 | 点击我完成了 |
| 完成反馈 | 首页 Overlay 成功卡片 | 约 1.2 秒自动关闭或点击关闭 |
| 当天已完成 | 首页已完成 | 查看记录或退出 |
| 第二天 | 重新进入未完成态 | 继续每日循环 |

### 5.2 状态字段（客户端至少需要）

| 字段 | 类型建议 | 说明 |
| --- | --- | --- |
| goalId | String / Long | 当前目标唯一标识 |
| goalName | String | 如“走路 20 分钟” |
| startDate | Date | 目标开始日期 |
| reminderEnabled | Boolean | 提醒开关，创建时默认 true；权限拒绝或用户关闭后为 false |
| reminderTime | HH:mm | 每天提醒时间，默认 20:00 |
| todayCompleted | Boolean | 当天是否完成 |
| currentStreak | Int | 当前连续完成天数 |
| totalCompleted | Int | 累计完成天数 |
| longestStreak | Int | 历史最长连续天数 |
| completedDates | Set<Date> | 用于月历展示 |
| goalStatus | ACTIVE / ENDED | 当前是否仍在坚持 |
| activeSlot | Int? | ACTIVE 固定为 1、ENDED 为 null，用唯一索引保证全局最多一个 ACTIVE |
| gapDays | Int | 从昨天向前连续未完成的自然日数，最早统计到 startDate |
| durationDays | Int | 包含开始日和结束日/今天的持续自然日数 |

### 5.3 完成事件规则（P0）

- 同一 goalId 的同一自然日只能成功写入一次完成记录；允许同一天结束旧目标后创建并完成新目标。

- 点击完成后先本地落库，再刷新 UI；避免动画完成但数据未保存。

- 若今日已完成，重复打开 App 不得再次显示可完成按钮。

- currentStreak：今天已完成时计算截至今天；今天未完成但昨天已完成时显示截至昨天；今天和昨天均未完成时为 0；中断后下一次完成从 1 开始。

- totalCompleted：每个完成日期只累计一次，永不因为中断归零。

- longestStreak：仅当 currentStreak 超过历史值时更新。

- durationDays：已结束目标为 endDate - startDate + 1，进行中目标为 today - startDate + 1，最小为 1；必须与 totalCompleted 分开展示。

- ACTIVE_INTERRUPTED：目标不是今天新建、今天未完成且昨天没有 Completion；连续漏掉多天只增加 gapDays，不新增状态。

### 5.4 日期边界

- 以用户设备本地自然日为准，00:00 进入新的一天。

- App 跨天仍在前台时，应在重新回到前台/页面刷新时检查日期变化并刷新 todayCompleted。

- V1 暂不支持补签历史日期。

- 用户修改系统时间可能造成边界异常，V1 可按设备日期处理，不需要增加复杂防作弊逻辑。

## 6. 提醒通知

| 项 | 要求 |
| --- | --- |
| 权限 | 首次需要通知时再请求系统通知权限，避免 App 一启动立即弹权限。 |
| 默认提醒 | 创建时默认开启，默认时间 20:00；用户可关闭，关闭时不申请权限。V1 仅一个固定提醒。 |
| 通知标题 | 今天那件事，做了吗？ |
| 通知内容 | 显示当前目标，例如：走路 20 分钟。 |
| 点击通知 | 有 ACTIVE 目标时进入当前首页；没有 ACTIVE 目标时进入创建页，不恢复旧目标。 |
| 已完成后 | 当天如已完成，后续同日提醒应取消/不再触发。 |
| 关闭提醒 | 设置关闭后立即取消已排程提醒。 |
| 时间精度 | 文案口径为“在所选时间附近提醒”；正常条件验收容差约 ±15 分钟，不申请精确闹钟特殊权限。 |
| 系统变化 | 手机重启、系统时间变化、时区变化或 App 更新后重新读取 ACTIVE 目标并调度。 |

## 7. 动效与反馈（P1，不阻塞核心功能）

| 动作 | 推荐效果 | 时长 |
| --- | --- | --- |
| 点击“我完成了” | 按钮轻微缩放 0.97 → 1.0 + 轻触觉反馈 | 120–180ms |
| 进入完成反馈 | 遮罩淡入 + 成功卡片轻微上浮/缩放 | 180–260ms |
| 成功对勾 | 缩放或描边出现 | 250–400ms |
| 反馈结束 | Overlay 淡出，首页呈现已完成状态 | 约 1.2s 后 |
| 页面切换 | 普通 Android 平滑转场即可，不做强烈左右飞入 | 180–250ms |

> 原则：完成感要有，但不要游戏化。动画不能影响用户快速退出 App。

## 8. 组件化建议（前端实现）

- AppScaffold：统一背景、安全区、页面左右间距。

- PrimaryButton：统一高度、圆角、禁用态、黄色/绿色两种状态。

- TopBar：返回、标题、设置入口统一点击热区。

- GoalHeader：今天 + 目标标题 + 图标。

- StreakBlock：当前连续天数与辅助说明。

- WeekProgress：7 日进度点 + 星期。

- StatsCard：当前连续 / 累计完成 / 最长连续。

- SettingRow：设置列表统一行高、箭头、分割线。

- SuccessFeedback：完成遮罩 + 成功卡片。

> 不要为 11 个页面分别写 11 套样式。页面越少越应该组件化，否则后续视觉调整成本反而更高。

## 9. 异常与边界场景

| 场景 | 预期处理 | 优先级 |
| --- | --- | --- |
| 目标名称为空 | 开始坚持按钮禁用；不允许创建空目标。 | P0 |
| 目标文字过长 | 单行最多 20 个用户可见字符；去除首尾空白，不允许纯空白和换行；首页最多 2 行。 | P0 |
| 通知权限拒绝 | 目标仍可正常创建；设置页显示提醒未开启，引导后续开启。 | P0 |
| 当天重复点击完成 | 客户端防抖 + 数据层唯一日期约束，不能重复累计。 | P0 |
| App 被杀后重新打开 | 从本地状态恢复，不应回到欢迎页或丢失连续记录。 | P0 |
| 跨天 | 重新计算今日状态，昨日记录保留。 | P0 |
| 结束目标 | 历史数据保留，不物理删除。 | P0 |
| 无历史目标 | 我的历史展示空状态，不显示“清空历史”。 | P1 |
| 月历切月 | 切换月份只读取历史数据，不改变当前连续数据。 | P1 |
| 提醒时间已过 | 创建或修改为当天已经过去的时间时，下一次安排在次日，不补发。 | P0 |
| 同日更换目标 | 旧 Goal 与新 Goal 分别允许一条当日 Completion，统计按 goalId 隔离。 | P0 |

## 10. 开发调整顺序

| 阶段 | 任务 | 完成标准 |
| --- | --- | --- |
| 第 1 步 | 统一全局颜色、字体、圆角、边距、按钮组件 | 所有页面不再出现明显不同的视觉体系。 |
| 第 2 步 | 完成 03/05 两个首页状态 | 未完成/已完成切换正确，主按钮与进度正确。 |
| 第 3 步 | 完成创建与首次流程 | 安装后 30 秒内能创建并进入首页。 |
| 第 4 步 | 完成记录页 + 连续/累计逻辑 | 所有统计值和月历日期一致。 |
| 第 5 步 | 完成设置、结束目标、历史 | 支链路无死路、返回逻辑正确。 |
| 第 6 步 | 补完成反馈、中断状态、通知 | 状态完整，提醒可控。 |
| 第 7 步 | 多机型走查 | 至少小屏/主流/大屏三档无裁切与遮挡。 |

### 10.1 实施记录（2026-09-03）

- `01 欢迎页` 已按确认文案和单一主操作完成首批真机对齐。
- `02 创建目标` 已接入创建前提醒开关与时间选择；默认开启、默认 `20:00`，关闭时隐藏时间入口。创建操作会先保存提醒状态再创建 Goal，保证关闭时不触发通知权限申请。
- 提醒卡片使用整行单一 Switch 语义，解决父子点击目标造成的双重切换，并加入 Compose 回归测试。
- `03 首页·未完成`、`05 首页·已完成`、`07 中断后首页` 已完成首批结构调整；统计详情收回记录页，首页只保留当天任务、连续状态、7 日进度和主操作。
- `04 完成反馈` 已实现深色遮罩 + 白色成功卡片，并保留 1.2 秒自动关闭、点击关闭及系统返回优先关闭；完成状态不进入独立页面栈。
- `06 坚持记录` 已完成月历、统计与中性漏做状态精调；`08 设置` 已改为独立圆角卡片列表；`09 结束确认` 已改为 Scaffold 内全屏确认层，避免平台 Dialog 改变 HyperOS 系统栏颜色。
- `10 结束结果` 与 `11 我的历史` 已加入居中最大宽度、纵向滚动和大字体保护；历史卡片分行展示统计，并按设置/结束结果/创建页来源显示正确返回文案。
- 所有核心页面主体使用 `520dp` 最大内容宽度，完成反馈卡片使用 `420dp`；已修正 Compose 约束顺序，确保最大宽度在宽屏和真横屏下生效。
- 新增 8 项自适应 Compose 测试，覆盖 320×480dp + 1.5 倍字体、360×320dp 短高度、记录、设置、历史、结束结果、结束确认和完成 Overlay；API 34 真机完整回归为 16/16 通过。
- Xiaomi API 34 真机已完成 `2400×1080` 真横屏欢迎/创建流程验证：内容区居中，底部操作可滚动访问，输入框和提醒卡片无横向拉伸或裁切。
- `我完成了` 按钮已加入 140ms、`1.0 → 0.97 → 1.0` 的按压缩放；成功触觉只在 Completion 落库成功并显示反馈层后触发，失败或重复完成不产生成功反馈。
- 完成 Overlay 已实现 180ms 遮罩淡入淡出，以及 240ms 卡片透明度、`0.94 → 1.0` 缩放和轻微上浮；960ms 展示后先退出再移除，总时长约 1.2 秒。点击关闭与自动关闭共用同一状态门控，不会重复回调。
- 新增 3 项完成反馈真机测试，覆盖按钮单次回调、点击退出后单次关闭、自动结束后单次关闭；API 34 专项结果为 3/3 通过，完整设备回归为 19/19 通过。
- 手工流程已抓取完成后的 250ms 动效中间帧和最终已完成状态：遮罩、成功卡片及文案完整，约 1.2 秒后正确衔接 `今天已完成 / 连续第 1 天`，未出现重复完成入口。测试数据和辅助包已清理。
- TalkBack 语义已覆盖创建目标输入、提醒时间、设置 Switch、页面返回、月份切换、7 日进度、月历日期状态、三项统计、完成对勾与 GitHub 外链；装饰箭头不再独立朗读。
- `关于一件` 已补齐 `520dp` 最大居中宽度；新增 6 项无障碍与布局测试，覆盖 600dp 欢迎/关于、320×480dp + 2.0 倍字体和核心语义，完整 API 34 回归为 25/25 通过。
- 真机通过临时 `288dpi` 映射完成 600dp 欢迎页视觉走查，内容区居中且按钮不横向拉满；走查后已恢复物理 `440dpi`、字体 `1.0` 和原开发者设置。发布前仍建议在物理平板/折叠屏及人工 TalkBack 手势下补一次最终签字验收。
- 提醒执行已补设备级自动化：使用真实 Room/DataStore、`NotificationManager` 和 `AlarmManager` 验证未完成时发通知并登记下一次 Alarm、完成后跳过当天、关闭时取消，以及开机/更新/日期/时间/时区变化后的重排入口；正式 `ReminderReceiver` 继续保持不可导出。
- 通知点击增加明确首页重置事件，修复应用原本停在设置/记录页时复用 Activity 后不回首页的问题；有 ACTIVE 时回首页，无 ACTIVE 时进入创建页。新增 5 项 API 34 提醒专项测试，且未修改真机系统时间。
- 本记录表示第二批实现与工程回归已落地；最终验收清单仍保持未勾选，待 M2 发布前在目标设备矩阵完成正式验收后统一签字确认。
- 2026-09-03 已在 API 36 模拟器以精确 `360 × 800dp` 视口完成 11 个页面/状态的同画布逐页比对；未发现裁切、溢出、安全区冲突或阻塞级视觉偏差。证据保存在 `build/ui-audit-360x800-2026-09-03/`，动态内容和已确认产品修正不按静态示例数据机械复刻。
- 设备迁移审计补齐 Preferences DataStore 的 `files/datastore/` 路径；新增 2 项打包资源测试后，API 36 完整设备回归为 32/32。真实 Setup Wizard 设备迁移仍保留为发布前人工验收项。

## 11. 最终验收清单

- [ ] 首次安装打开后只出现欢迎页，不出现登录/注册。

- [ ] 创建目标不超过 30 秒即可完成。

- [ ] 首页日常完成操作最多 1 次点击即可完成。

- [ ] 当天完成后不可重复增加累计完成次数。

- [ ] 第二天自动恢复为新的未完成状态。

- [ ] 今天未完成但昨天已完成时展示截至昨天的 currentStreak；昨天也未完成时归 0，下一次完成从 1 开始；累计数据不丢。

- [ ] 月历完成日期与本地完成记录完全一致。

- [ ] 7 日窗口固定为过去 6 天 + 今天，开始日前和未来日期不误标为未完成。

- [ ] 设置入口、返回、记录、历史等所有图标热区至少 48dp × 48dp。

- [ ] 结束目标后历史仍可查看，且可创建下一件事。

- [ ] 通知关闭后不再弹出；完成后当天不重复提醒。

- [ ] 创建页提醒默认开启且时间为 20:00；关闭后不申请通知权限。

- [ ] 手机重启、系统时间/时区变化或 App 更新后，提醒状态与 ACTIVE 目标一致且无重复任务。

- [ ] 结束结果和历史中的“持续天数”按包含首尾日期计算，并与“完成天数”分开展示。

- [ ] 完成反馈作为首页 Overlay 实现，不产生独立返回页。

- [ ] 历史卡片不裁切第三行，从设置和结束结果进入时分别返回正确来源。

- [ ] 360dp 基准、小屏和大屏均无文字裁切、按钮遮挡。

- [ ] 底部按钮不与系统手势区冲突。

- [ ] 页面背景、字体、圆角、主色全部符合统一规范。

- [ ] 页面 Token 使用背景 #FDFBF7、完成绿 #59AD5E、待完成黄 #F5B838、危险红 #E05252、主文字 #141414。

- [ ] 未出现多余功能入口：社区、排行、积分、AI、复杂统计。

## 12. 交付说明

- `design/screens/` 的 11 张 360 × 800 PNG 与 Figma 作为结构和视觉参考；本文和开发决策基线作为客户端实现与验收口径。

- 如果 Figma 与本文存在不一致：优先遵循本文的产品逻辑；视觉细节以最终确认后的高保真稿为准。

- 开发过程中如发现需要新增功能才能解决问题，先记录问题，不直接扩需求；优先用现有极简模型解决。

- 所有新增交互都要满足一个判断：是否能直接帮助用户今天完成这件事。不能，则 V1 不加。

> V1 封板目标：创建一件事 → 每天完成 → 看记录 → 中断后继续 → 结束 → 开始下一件事。
